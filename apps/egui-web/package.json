{
  "name": "egui-web",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "test": "playwright test",
    "test:pw-legacy": "playwright test",
    "test:bdd": "cucumber-js --config cucumber.ts",
    "typecheck": "tsc --noEmit",
    "test:preflight": "pnpm run typecheck && node --test test-node/*.test.cjs"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^12.8.1",
    "@playwright/test": "^1.57.0",
    "@types/node": "^25.6.0",
    "playwright": "^1.57.0",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.3"
  }
}
