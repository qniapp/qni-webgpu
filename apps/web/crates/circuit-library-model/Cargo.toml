[package]
name = "qni-web-circuit-library-model"
version = "0.1.0"
edition = "2021"

[dependencies]
js-sys = "0.3"
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
trybuild = "1"
