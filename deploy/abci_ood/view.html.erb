<a href="/node/<%= host %>/<%= port %>/">Connect to QNI WebGPU</a>
<% if context.auth_mode.to_s == "password" || context.auth_mode.to_s == "htpasswd_file" %>
<p>Basic authentication is enabled for this session.</p>
<% end %>
