- The current best actual branch run is `25161542044` with `web=85s`, `tui=36s`, `mcp=13s`. Any new hypothesis should beat that on real CI, not just on the proxy.
- `trunk build` still spends about 8s on CI and continues to download/install `wasm-bindgen` on ephemeral runners. Revisit Trunk tool caching only if the benchmark is extended to model the current no-cache behavior more faithfully; the simple `actions/cache` add-on looked worse under the present proxy because local trunk builds are already warm.
- Playwright 6 workers and Cucumber CI parallelism=2 are now the validated web-test settings. Higher worker counts or more Cucumber parallelism are low-priority unless fresh CI evidence suggests the web lane is still leaving parallelism on the table.
