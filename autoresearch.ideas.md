- Investigate whether `dtolnay/rust-toolchain` is still the best trade-off now that warm-cache CI runs are dominated by `Setup Rust` (~12s web, ~11s tui). GitHub-hosted runners already ship Rust, so verify whether a lighter preinstalled-toolchain path can preserve the same stable/tooling guarantees without paying the full rustup update every run.
- `Web trunk build` is now mostly `wasm-bindgen` download/install overhead after the warm Rust cache hit. Check whether preinstalling or caching that tool can remove the remaining ~2-3s without changing the built artifact or test coverage.
- Keep the CI Playwright worker cap at 4 for now. A 6-worker local CI-like run was a bit faster, but the projected/actual evidence was too weak to justify another push; only revisit higher worker counts if a stronger CI-side signal appears.
