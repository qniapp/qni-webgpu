- The current best actual branch result remains the split-preflight topology, now re-restored at commit `7fe25f6` after the failed split-BDD detour. Real CI run `25167426445` (same topology as `4b48fb2` / `7fe25f6`) reached roughly `web=78s`, `web-preflight=14s`, `tui=36s`, `mcp=18s`, clearly beating the restored-6-worker no-install branch (`c1ee675`, run `25164643080`, ~`web=87s`, `tui=38s`, `mcp=15s`). Any new hypothesis should beat this real CI baseline, not just the proxy.
- Trunk tool-cache experiments are currently stale: both explicit `actions/cache` for `~/.cache/trunk` and explicit system `wasm-bindgen` install were tried, and neither produced a better real CI result than the no-install baseline. Only revisit this lane if a materially different Trunk-tool strategy emerges.
- Playwright 6 workers and Cucumber CI parallelism=2 are the current validated web-test settings. Higher worker counts, the old 4-worker rollback, viewport shrinking, faster helper polling, and raw post-drag sleep reduction are stale/low-priority: each either regressed under the corrected proxy or became flaky under repeated CI-like runs.
- The same-job parallel web-suites lane is also stale for now. Running BDD and legacy concurrently against one shared static server looked attractive locally, but both the plain version and the reduced-contention version (`QNI_EGUI_WEB_CUCUMBER_PARALLEL=1`) were flaky under repeated CI-like verification before reaching real CI.
- Benchmark integrity has improved materially (exact workflow matching, synthetic-step filtering, median timings, median cold Trunk samples, corrected local web-suite harness, median warmed local commands, runtime-file exact matching, direct reuse of exact observed CI run-step durations, and workflow-dirty reuse of observed run-step durations when runtime files still match). The proxy is now anchored much closer to real CI (~`74s` projected for the split-preflight candidate versus ~`78s` actual on run `25167426445`) instead of the old unrealistic ~30s range.
- The next promising lane is therefore either another benchmark-quality gap around setup/topology changes when no exact runtime baseline exists, or a completely different CI bottleneck with larger upside than the recent web-test micro-tuning and concurrency experiments. The obvious small next target inside the web lane is the remaining `Web Playwright legacy (static dist)` ~43s block, but only pursue ideas that avoid the already-invalidated routes (worker fan-out, raw wait shrinking, same-job parallel suites, or duplicated-build fan-out like the reverted `web-bdd` split that regressed badly on real CI run `25167832195`).
