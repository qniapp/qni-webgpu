- The current best actual branch result remains the split-preflight topology, now re-restored at commit `7fe25f6` after the failed split-BDD detour. Real CI run `25167426445` (same topology as `4b48fb2` / `7fe25f6`) reached roughly `web=78s`, `web-preflight=14s`, `tui=36s`, `mcp=18s`, clearly beating the restored-6-worker no-install branch (`c1ee675`, run `25164643080`, ~`web=87s`, `tui=38s`, `mcp=15s`). Any new hypothesis should beat this real CI baseline, not just the proxy.
- Trunk tool-cache experiments are currently stale: both explicit `actions/cache` for `~/.cache/trunk` and explicit system `wasm-bindgen` install were tried, and neither produced a better real CI result than the no-install baseline. Only revisit this lane if a materially different Trunk-tool strategy emerges.
- Playwright 6 workers and Cucumber CI parallelism=2 are the current validated web-test settings. Higher worker counts, the old 4-worker rollback, viewport shrinking, faster helper polling, and raw post-drag sleep reduction are stale/low-priority: each either regressed under the corrected proxy or became flaky under repeated CI-like runs.
- The same-job parallel web-suites lane is also stale for now. Running BDD and legacy concurrently against one shared static server looked attractive locally, but both the plain version and the reduced-contention version (`QNI_WEB_CUCUMBER_PARALLEL=1`) were flaky under repeated CI-like verification before reaching real CI.
- Benchmark integrity has improved materially (exact workflow matching, synthetic-step filtering, median timings, median cold Trunk samples, corrected local web-suite harness, median warmed local commands, runtime-file exact matching, direct reuse of exact observed CI run-step durations, workflow-dirty reuse of observed run-step durations when runtime files still match, conservative handling of duplicated compile-heavy jobs, per-job hidden-overhead modeling, and bounded retry while collecting median local samples). The proxy is now far closer to real CI than the old unrealistic ~30s range. With the current exact matching run set (`25169323225`, `25168767233`, `25168329696`, `25167426445`), the honest split-preflight proxy baseline is now about `84s` rather than the older `~80s` figure.
- The next promising lane is therefore either another benchmark-quality gap around setup/topology changes when no exact runtime baseline exists, or a completely different CI bottleneck with larger upside than the recent web-test micro-tuning and concurrency experiments. The obvious remaining target inside the web lane is the `Web Playwright legacy (static dist)` ~43s block, but only pursue ideas that avoid the already-invalidated routes (worker fan-out, raw wait shrinking, same-job parallel suites, duplicated-build fan-out like the reverted `web-bdd` split on real CI run `25167832195`, tiny success-only screenshot-write removals that already showed only ~30ms isolated local upside before two benchmark crashes, and favicon/404 cleanup which functionally works but is now fully pruned as a non-competitive runtime lane).
- The bounded-attempt median sampler is now validated as a benchmark robustness improvement, not a metric regression: a control rerun without it still landed at the same `84s` proxy baseline once run `25169323225` entered the exact matching set. Do not re-discard that tweak based solely on the old `84s vs 80s` comparison.
- The old `observed_ci_run_id=0` / `observed_ci_run_count=0` anomaly is now much better bounded. The proxy emits `observed_ci_selection_tier`, `observed_ci_matching_run_count`, `observed_ci_exact_runtime_run_count`, `observed_ci_exact_run_runtime_count`, and `observed_ci_view_failure_count`, and the workflow-match-only tier now models run steps conservatively via `max(observed matching-run cost, local run cost)`. A controlled dirty favicon reproduction now projects about `81s` at selection tier `1` instead of collapsing to a fake ~`28s` win, so workflow-only runtime-dirty candidates should no longer get false green lights from optimistic local timings.
