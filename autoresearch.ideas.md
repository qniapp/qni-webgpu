- Current corrected baseline: split-preflight topology with exact proxy around `80s`; recent real CI run `25199557265` had `web=87s`, and list-reporter diagnostic run `25199813669` had `web=85s`, `legacy=44s`.
- Stale/pruned lanes: duplicated-build `web-bdd` split (`25167832195`), renamed `web-build` artifact+legacy-shard fan-out (`25197655273`, `web-build=92s`, `trunk build=68s`), job-name-preserving artifact fan-out (`25198506199`, wall≈98s), one shared static server for BDD+legacy in the `web` job (`25199074002`, no win), frame-based replacement for fixed 50ms drag waits (`25199398047`, exact proxy `87s`), permanent Playwright list reporter (`25199813669`, useful timings but exact proxy `83s`), same-job BDD+legacy parallelism, Playwright worker fan-out above 6 (8 workers caused local startup timeouts), lower Playwright workers (4 workers timed out locally), viewport shrink, raw wait shrink, screenshot-write removal, favicon cleanup, explicit Trunk tool cache, explicit system `wasm-bindgen` install.
- CI per-test evidence from run `25199813669`: slowest legacy tests were `egui webgpu canvas renders content` 24.3s, `dragging does not grow state vector until drop` 21.9s, `H on q0 and q1` 17.7s, `placed circuit gate keeps its visual` 17.4s, and several drag visual tests around 15-17s. Sum of reported per-test durations was about 204.5s; with 6 workers this explains the ~44s legacy wall time.
- Benchmark integrity now includes exact workflow/runtime matching, conservative workflow-only runtime modeling, compile-heavy renamed-job penalty, per-job overhead, fallback same-step browser-suite costs, bounded median retries, fixed local static-server readiness probes, and retired metric compatibility for `web_static_suites_s=0`.
- Promising next path: target a high-cost test while preserving assertions, especially reducing page startup/render cost in `egui webgpu canvas renders content` or `dragging does not grow state vector until drop`. Avoid worker/wait micro-tuning without stronger evidence.
