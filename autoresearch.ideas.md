- Current corrected baseline: split-preflight topology on `7155768`/`445dd1f` family with proxy around `79-81s`; recent real CI runs: `25198795807` (`web=81s`) and shared-server candidate `25199074002` (`web=81s`).
- Stale/pruned lanes: duplicated-build `web-bdd` split (`25167832195`), renamed `web-build` artifact+legacy-shard fan-out (`25197655273`, `web-build=92s`, `trunk build=68s`), job-name-preserving artifact fan-out (`25198506199`, wall≈98s, `web=35s` then `web-legacy=53s`), one shared static server for BDD+legacy in the `web` job (`25199074002`, no win: combined step `55s` vs prior BDD+legacy `54s`), same-job BDD+legacy parallelism, Playwright worker fan-out above 6, viewport shrink, raw wait shrink, screenshot-write removal, favicon cleanup, explicit Trunk tool cache, explicit system `wasm-bindgen` install.
- Benchmark integrity now includes exact workflow/runtime matching, conservative workflow-only runtime modeling, compile-heavy renamed-job penalty, per-job overhead, fallback same-step browser-suite costs, bounded median retries, and fixed local static-server readiness probes. Future topology experiments should inspect selection tier and compare against real CI before trusting fallback wins.
- Promising next path: avoid more topology churn unless the proxy win survives the stricter fallback model; look instead for a real legacy Playwright bottleneck that preserves assertions (e.g. sample/screenshot helper cost, page startup reuse alternatives, or WebGPU-specific browser launch overhead).
- Another possible lane: improve benchmark observability by capturing per-test CI Playwright JSON/profile artifacts, but do not replace the existing Web legacy Playwright validation.
