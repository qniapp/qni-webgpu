- Current corrected baseline: split-preflight topology with exact proxy around `79s`; recent real CI runs include `25199184620` (`web=84s`, median exact proxy `79s`).
- Stale/pruned lanes: duplicated-build `web-bdd` split (`25167832195`), renamed `web-build` artifact+legacy-shard fan-out (`25197655273`, `web-build=92s`, `trunk build=68s`), job-name-preserving artifact fan-out (`25198506199`, wall≈98s), one shared static server for BDD+legacy in the `web` job (`25199074002`, no win: combined step `55s` vs prior `54s`), frame-based replacement for fixed 50ms drag-settling waits (`25199398047`, exact proxy `87s`, `web=89s`), same-job BDD+legacy parallelism, Playwright worker fan-out above 6, lower Playwright workers (4 workers timed out locally before CI), viewport shrink, raw wait shrink, screenshot-write removal, favicon cleanup, explicit Trunk tool cache, explicit system `wasm-bindgen` install.
- Benchmark integrity now includes exact workflow/runtime matching, conservative workflow-only runtime modeling, compile-heavy renamed-job penalty, per-job overhead, fallback same-step browser-suite costs, bounded median retries, and fixed local static-server readiness probes. Future topology/test-runtime experiments should inspect selection tier and compare against real CI before trusting fallback wins.
- Promising next path: capture CI per-test Playwright timing/profile artifacts before more legacy test micro-tuning; local timings diverge too much from GitHub's software WebGPU runner.
- Another possible lane: investigate screenshot/sample helper cost with evidence from CI per-test timings, but do not remove existing visual assertions or screenshot validations.
