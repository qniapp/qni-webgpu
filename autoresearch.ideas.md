- Current corrected baseline: split-preflight topology on `ffe3ceb`/`c4df55f` with proxy `projected_ci_s≈76.5s`; real CI run `25198110827` had `web=73s`, `web-preflight=14s`, `tui=33s`, `mcp=13s`.
- Stale/pruned lanes: duplicated-build `web-bdd` split (`25167832195`), renamed `web-build` artifact+legacy-shard fan-out (`25197655273`, `web-build=92s`, `trunk build=68s`), job-name-preserving artifact fan-out (`25198506199`, wall≈98s, `web=35s` then `web-legacy=53s`), same-job BDD+legacy parallelism, Playwright worker fan-out above 6, viewport shrink, raw wait shrink, screenshot-write removal, favicon cleanup, explicit Trunk tool cache, explicit system `wasm-bindgen` install.
- Benchmark integrity now includes exact workflow/runtime matching, conservative workflow-only runtime modeling, compile-heavy renamed-job penalty, per-job overhead, bounded median retries, and fixed local static-server readiness probes. Future topology experiments should inspect selection tier and compare against real CI before trusting large fallback wins.
- Promising next path: improve fallback/new-downstream-job overhead modeling using observed generic Node job overhead, because artifact fan-out was still too optimistic even when the compile-heavy job name was preserved.
- Another possible lane: search for a non-topology legacy Playwright bottleneck with larger upside than micro-tuning, but avoid changing test semantics or product behavior.
