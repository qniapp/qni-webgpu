- Current corrected baseline: split-preflight topology with exact proxy around `76s`; latest real web job after BDD parallelism 3 was `25200809846` with `web=78s`, BDD `9s`, legacy `42s`.
- Kept runtime wins: `fec3402` trimmed duplicate Bell-state interactions from the canvas-render smoke; `028dc9e` reduced the drag-growth test to one setup gate; `372bfa7` runs the three BDD scenarios with parallelism 3 so they fit in one wave.
- Stale/pruned lanes: duplicated-build `web-bdd` split, renamed/job-name-preserving artifact fan-out, one shared static server for BDD+legacy, frame-based drag waits, permanent Playwright list reporter, same-job BDD+legacy parallelism, Playwright worker fan-out above 6 (8 workers local startup timeouts), lower Playwright workers (4 workers timed out locally), tiny BDD visible-wait cleanup (proxy worsened), removing the intermediate single-H assertion from the two-H test (proxy worsened), source-order scheduling of the placed-gate visual test (proxy worsened), skipping state-vector readiness in pure visual Playwright tests (proxy worsened), folding the two-H state test into canvas smoke (benchmark-path crash despite one manual pass), removing the placed-gate redundant wait (proxy worsened), viewport/raw-wait/screenshot/favicon cleanup, explicit Trunk cache, explicit system `wasm-bindgen` install.
- CI per-test evidence from diagnostic run `25199813669`: slowest legacy tests before trims were `egui webgpu canvas renders content` 24.3s, `dragging does not grow state vector until drop` 21.9s, `H on q0 and q1` 17.7s, `placed circuit gate keeps its visual` 17.4s, and several drag visual tests around 15-17s.
- Benchmark integrity includes exact workflow/runtime matching, conservative workflow-only runtime modeling, compile-heavy renamed-job penalty, per-job overhead, fallback same-step browser-suite costs, bounded median retries, fixed local static-server readiness probes, and retired metric compatibility for `web_static_suites_s=0`.
- Promising next path: inspect `placed circuit gate keeps its visual while dragging another gate` or other visual tests for duplicated setup, but avoid removing unique visual/assertion coverage.
