- Current corrected baseline: split-preflight topology with exact proxy around `76s` after trimming duplicate legacy-test work. Real CI runs: `25200168055` (`web=79s`, legacy `40s`) and `25200415664` (`web=80s`, BDD `9s`, legacy `40s`).
- Kept runtime wins: `fec3402` trimmed duplicate Bell-state interactions from the canvas-render smoke while preserving dedicated H/CNOT/control tests; `028dc9e` reduced the drag-growth test to one setup gate while preserving the delayed-growth invariant.
- Stale/pruned lanes: duplicated-build `web-bdd` split (`25167832195`), renamed `web-build` artifact+legacy-shard fan-out (`25197655273`, `web-build=92s`, `trunk build=68s`), job-name-preserving artifact fan-out (`25198506199`, wall≈98s), one shared static server for BDD+legacy (`25199074002`, no win), frame-based replacement for fixed 50ms drag waits (`25199398047`, exact proxy `87s`), permanent Playwright list reporter (`25199813669`, useful timings but exact proxy `83s`), same-job BDD+legacy parallelism, Playwright worker fan-out above 6 (8 workers caused local startup timeouts), lower Playwright workers (4 workers timed out locally), viewport shrink, raw wait shrink, screenshot-write removal, favicon cleanup, explicit Trunk tool cache, explicit system `wasm-bindgen` install.
- CI per-test evidence from run `25199813669`: slowest legacy tests before trims were `egui webgpu canvas renders content` 24.3s, `dragging does not grow state vector until drop` 21.9s, `H on q0 and q1` 17.7s, `placed circuit gate keeps its visual` 17.4s, and several drag visual tests around 15-17s.
- Benchmark integrity includes exact workflow/runtime matching, conservative workflow-only runtime modeling, compile-heavy renamed-job penalty, per-job overhead, fallback same-step browser-suite costs, bounded median retries, fixed local static-server readiness probes, and retired metric compatibility for `web_static_suites_s=0`.
- Promising next path: inspect `H on q0 and q1 yields uniform superposition` or `placed circuit gate keeps its visual while dragging another gate` for similarly duplicated setup, but avoid removing unique visual/assertion coverage.
