- Current corrected baseline: split-preflight topology with clipped pixel-sampling exact CI evidence now includes all successful GitHub rerun attempts instead of only the latest attempt per run. Run `25201875826` first pass had `web=72s`, BDD `12s`, legacy `37s`, preflight `14s`, MCP `16s`, TUI `38s`, exact proxy `70s`; its rerun had `web=74s`, preflight `18s`, MCP `16s`, TUI `31s`, exact proxy `73s`; pushed evidence run `25202694641` on `791c34c` had `web=76s`, preflight `14s`, MCP `15s`, TUI `33s`, exact proxy `73.5s`. The all-attempt proxy baseline is now `projected_ci_s=71` with real CI range `web≈72–76s`.
- Kept runtime wins: `fec3402` trimmed duplicate Bell-state interactions from the canvas-render smoke; `028dc9e` reduced the drag-growth test to one setup gate; `372bfa7` runs the three BDD scenarios with parallelism 3 so they fit in one wave; `9dcd055` clips `sampleCanvasPixels` screenshots to probe bounds instead of encoding full-canvas PNGs.
- Stale/pruned lanes: duplicated-build `web-bdd` split, renamed/job-name-preserving artifact fan-out, one shared static server for BDD+legacy, frame-based drag waits, permanent Playwright list reporter, same-job BDD+legacy parallelism, Playwright worker fan-out above 6 (8 workers local startup timeouts; 7 workers proxy worsened after clipped sampling), lower Playwright workers (4 workers timed out locally), tiny BDD visible-wait cleanup (proxy worsened), removing the intermediate single-H assertion from the two-H test (proxy worsened), source-order scheduling of the placed-gate visual test (proxy worsened), skipping state-vector readiness in pure visual Playwright tests (proxy worsened), folding the two-H state test into canvas smoke (benchmark-path crash despite one manual pass), removing the placed-gate redundant wait (proxy worsened), DOMContentLoaded navigation before readiness waits (worse than exact 70s baseline), removing BDD-owned state-panel z-order legacy duplicate (worse than exact 70s baseline), removing BDD-owned plain-Chromium legacy duplicate (local improved but proxy worse than exact baseline), clipping broad render-content screenshots to a top band (web_local worsened), merging palette panel/control visual tests (no local/proxy win), removing success-only canvas smoke screenshot artifacts after clipped sampling (local improved but conservative proxy worse than exact baseline), viewport/raw-wait/screenshot/favicon cleanup, explicit Trunk cache, explicit system `wasm-bindgen` install.
- CI per-test evidence from diagnostic run `25199813669`: slowest legacy tests before trims were `egui webgpu canvas renders content` 24.3s, `dragging does not grow state vector until drop` 21.9s, `H on q0 and q1` 17.7s, `placed circuit gate keeps its visual` 17.4s, and several drag visual tests around 15-17s.
- Benchmark integrity includes exact workflow/runtime matching, all-successful-attempt GitHub rerun sampling, conservative workflow-only runtime modeling, compile-heavy renamed-job penalty, per-job overhead, fallback same-step browser-suite costs, bounded median retries, fixed local static-server readiness probes, and retired metric compatibility for `web_static_suites_s=0`.
- Promising next path: no high-confidence runtime experiment remains after clipped pixel sampling and the subsequent pruned lanes. Future runtime candidates should beat the stricter all-attempt proxy baseline (`projected_ci_s=71`) before spending real CI. If continuing, first gather fresh CI per-test diagnostics or improve the benchmark model for runtime-dirty candidates; otherwise focus on PR review/cleanup for the kept clipped-screenshot change.
