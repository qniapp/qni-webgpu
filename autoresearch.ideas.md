- The current best actual branch result remains the no-install workflow: run `25161542044` reached `web=85s`, `tui=36s`, `mcp=13s`, and rerun `25162727553` reproduced an 85s wall clock. Any new hypothesis should beat that on real CI, not just on the proxy.
- Trunk tool-cache experiments are currently stale: both explicit `actions/cache` for `~/.cache/trunk` and explicit system `wasm-bindgen` install were tried, and neither produced a better real CI result than the no-install baseline. Only revisit this lane if a materially different Trunk-tool strategy emerges.
- Playwright 6 workers and Cucumber CI parallelism=2 are the current validated web-test settings. Higher worker counts, the old 4-worker rollback, or viewport shrinking are all low-priority unless fresh CI evidence suggests the web lane is still leaving parallelism on the table.
- Benchmark integrity has improved materially (exact workflow matching, synthetic-step filtering, median timings, median cold Trunk samples). The next promising lane is either another benchmark-quality gap of similar size or a genuinely different bottleneck with a larger upside than the recent web-test micro-tuning attempts.
- If we stay on the web lane, the most plausible unexplored micro-optimization is test-wait overhead in `apps/egui-web/features/support/egui-helpers.cjs` (100ms polling plus explicit `waitForTimeout` calls). Only pursue it with evidence that it helps CI-like runs without weakening readiness guarantees.
